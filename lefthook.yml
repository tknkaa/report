pre-commit:
  parallel: true
  jobs:
    - name: oxfmt
      glob: "*.{ts,tsx,json,jsonc}"
      run: pnpm oxfmt --write {staged_files}
      stage_fixed: true
    - name: eslint
      glob: "*.{ts,tsx}"
      run: pnpm oxlint --fix {staged_files}
      stage_fixed: true
